# Copy this file to server/.env and fill in your values

PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# The Odds API key (https://the-odds-api.com) — free tier: 500 req/month
# Leave empty to run in SIMULATED mode (no API calls)
ODDS_API_KEY=
